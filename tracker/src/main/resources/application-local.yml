spring:
  datasource:
    url: jdbc:mysql://localhost:3306/expense_app_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

server:
  port: 8081

keycloak:
  server-url: http://localhost:8080
  realm: expense-realm
  admin-client-id: expense-backend
  admin-client-secret: m1WS5Yur2UkUjxA3fnZasXiYLT05uBtY

jwt:
  auth:
    converter:
      resource-id: expense-client
      principle-attribute: preferred_username

logging:
  level:
    org.hibernate.SQL: debug