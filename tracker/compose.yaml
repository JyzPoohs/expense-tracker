services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=expense_be_db'
      - 'MYSQL_ROOT_PASSWORD=password'
    ports:
      - "3307:3306"

  redis:
    image: 'redis:latest'
    ports:
      - "6379:6379"